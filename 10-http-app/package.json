{
  "name": "10-http-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server server/db.json --port 30001"
  },
  "devDependencies": {
    "json-server": "^0.17.1",
    "vite": "^4.1.0"
  }
}
